<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="study.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="study.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Aug 01 13:56:32 CST 2019.
        -->
        <id column="ID" jdbcType="BIGINT" property="id" />
        <result column="TITLE" jdbcType="VARCHAR" property="title" />
        <result column="TAG" jdbcType="VARCHAR" property="tag" />
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate" />
        <result column="GMT_MODIFY" jdbcType="BIGINT" property="gmtModify" />
        <result column="CREATOR_ID" jdbcType="VARCHAR" property="creatorId" />
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount" />
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount" />
        <result column="LIKE_COUNT" jdbcType="INTEGER" property="likeCount" />
    </resultMap>
    <update id="incView" parameterType="study.community.model.Question">
        update QUESTION
        set
        VIEW_COUNT = VIEW_COUNT + #{viewCount,jdbcType=INTEGER}
        where id= #{id}
    </update>
    <update id="incComment" parameterType="study.community.model.Question">
        update QUESTION
        set
        COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
        where id= #{id}

    </update>

    <select id="selectRelated" parameterType="study.community.model.Question" resultMap="BaseResultMap">
        select * from QUESTION where id!=#{id} and tag regexp #{tag}
<!--        <where>-->
<!--            <if test="id != null">-->
<!--                and id != #{id}-->
<!--            </if>-->
<!--            <if test="tag != null and tag != ''">-->
<!--                and tag regexp #{tag}-->
<!--            </if>-->
<!--        </where>-->
<!--        order by gmt_create desc limit 20-->
    </select>

    <select id="countBySearch" parameterType="study.community.dto.QuestionSearchDTO"
            resultType="java.lang.Integer">
        select count(*) from QUESTION
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
        </where>
    </select>

    <select id="selectBySearch" parameterType="study.community.dto.QuestionSearchDTO"
            resultMap="BaseResultMap">
        select * from QUESTION
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
        </where>
        order by gmt_create desc limit #{page},#{size}
    </select>
</mapper>